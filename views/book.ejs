<%- include("partials/header.ejs"); %>

<div id="book" class="form">
    
    <div class="container">
        <h1><%= book.title %></h1>
        <p>Este livro tem <%= book.pageqty %> páginas.</p>
    </div>

    <div class="actions">
        <a href="/books/edit/<%= book.id %>" class="link">Editar livro</a>

        <form action="/books/remove/<%= book.id %>" method="POST">
            <input type="submit" value="remover" class="link-remover">
        </form>

        <a href="/books" class="link">Voltar</a>
    </div>
</div>

<%- include("partials/footer.ejs"); %>
